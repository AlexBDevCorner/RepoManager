# Task 22 — Remove repository

- Milestone: 4 — Read-only MVP
- Type: UI + backend

Provide `Remove from dashboard`. This must only remove it from `repositories.json`. It must never delete the folder, Git data, or remotes.

Confirmation text:

```text
Remove Store from Repo Dashboard?

The repository and its files will not be deleted.
```

## Acceptance criteria

- [ ] Entry removed from config and UI.
- [ ] Files on disk untouched.
